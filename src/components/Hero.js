import React from "react";
import Slick from "../constants/Slick";

const Hero = () => {
  return (
    <section className="hero-section mb-30">
      <Slick />
    </section>
  );
};

export default Hero;
